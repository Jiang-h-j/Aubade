# B02 切片进度

| 切片 | 状态 | 说明 |
|---|---|---|
| 01 R3 最近记录删除 | 未开始 | 记账页最近记录左滑删除 + 二次确认 |
| 02 R4 分类 Store 能力 | 未开始 | updateCategory / deleteCategory / 引用计数 + 单测 |
| 03 R4 分类管理 UI | 未开始 | 我的页分类可管理列表 + 编辑器（依赖 02） |

## 依赖与顺序

- 01 独立，可任意顺序先行。
- 02 → 03：03 消费 02 的 `updateCategory` / `deleteCategory` / `CategoryError`，02 先落。
- 推荐顺序：01 → 02 → 03，或 02 → 01 → 03。

## 备注

- 开放项已定：预置分类锁全部字段（禁删、禁改名、禁改图标/颜色）。
- 原型 `data.js:940` 删除统一转"其他"是 bug，真实端按方向兜底（收入转"其他收入"），已在 02/03 纠偏。
- 新增分类同方向重名判定放 03 UI 层（不改 02 的 `createCategory`）；编辑重名判定在 02 的 `updateCategory` 内建。
